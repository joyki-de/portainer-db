services:
  portainer-dashboard:
    build: .
    container_name: portainer-dashboard
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - PORTAINER_URL=https://85.x.x.x:9443
